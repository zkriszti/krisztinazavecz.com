---
import About from '../content/about.astro';

const { pathname } = Astro.url;

// Remove leading slash and convert to title case for component name
const pageName = pathname === '/' ? 'about' : pathname.slice(1).split('/')[0].charAt(0).toUpperCase() + pathname.slice(2).split('/')[0].slice(1);

---
<About />


